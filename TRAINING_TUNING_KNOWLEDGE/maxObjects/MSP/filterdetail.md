---
type: msp
name: "filterdetail"
summary: "Detail the characteristics of a filter"
signal: false
url: "https://docs.cycling74.com/reference/filterdetail/"
package: "MSP"
see_also: ["filterdesign", "plot~", "cascade~", "biquad~"]
---
# filterdetail

Detail the characteristics of a filter

## Description

Use the [filterdetail](https://docs.cycling74.com/reference/filterdetail) object to produce details from a filter specification provided as input. The filter specification may be generated by the [filterdesign](https://docs.cycling74.com/reference/filterdesign) object, or by other means. The output includes the impulse response, step response, magnitude and phase responses, as well as phase and group delay.

## Arguments

None.

## Attributes

### numpoints[int]

The resolution of the returned filter characteristics. You may reduce this to reduce the computational intensity of performing the detailing calculations, or increase it to gain greater resolution. By default, a resolution of 512 points is used.

### Common Box Attributes

Shared across all objects — see [Common Box Attributes](../_shared/common-box-attributes.md).

## Messages

### list

Specify a biquad filter using a list of five filter coefficients

Arguments:

- a0
  [float]
- a1
  [float]
- a2
  [float]
- b1
  [float]
- b2
  [float]

### dictionary

A filterdesign dictionary specifying a filter in terms of its zeroes, poles, and gain or as a transfer function. The transfer function is expressed as a fraction with the FIR coefficients composing the numerator and the IIR coefficients composing the denominator. The zero-pole-gain representation is preferred because it yields a more numerically accurate result.

Arguments:

- dictionary-name
  [symbol]

## Output

### list

- leftmost outlet -- magnitude response
- second outlet -- phase response
- third outlet -- phase delay
- fourth outlet -- group delay
- fifth outlet -- impulse response
- rightmost outlet -- step response

## See Also

| Name | Description |
| --- | --- |
| [filterdesign](https://docs.cycling74.com/reference/filterdesign) | Create a filter specification |
| [plot~](https://docs.cycling74.com/reference/plot~) | Visualize two-dimensional data |
| [cascade~](https://docs.cycling74.com/reference/cascade~) | Cascaded series of biquad filters |
| [biquad~](https://docs.cycling74.com/reference/biquad~) | Two-pole, two-zero filter |
