---
type: max
name: "deferlow"
summary: "Defer the execution of a message (always)"
signal: false
url: "https://docs.cycling74.com/reference/deferlow/"
package: "Max"
see_also: ["defer", "delay", "qlim", "uzi"]
---
# deferlow

Defer the execution of a message (always)

## Description

The [deferlow](https://docs.cycling74.com/reference/deferlow) object places all incoming messages at the tail of the low priority queue. This is unlike the  **defer**  object, however, which places high priority messages at the front of the low priority queue, and passes low priority messages immediately. The [deferlow](https://docs.cycling74.com/reference/deferlow) object is useful to preserve message sequencing that might otherwise be reversed with the [defer](https://docs.cycling74.com/reference/defer) object and/or guarantee that an incoming message will be deferred to a future servicing of the low priority queue even if that message is low priority itself.

#### Discussion

Examples of high priority messages are those generated by a MIDI object (such as [notein](https://docs.cycling74.com/reference/notein)) or a timing object (such as [metro](https://docs.cycling74.com/reference/metro) or [seq](https://docs.cycling74.com/reference/seq)), and examples of low priority message are those generated in response to user events (such as clicking a [button](https://docs.cycling74.com/reference/button)).

## Arguments

None.

## Attributes

### Common Box Attributes

Shared across all objects — see [Common Box Attributes](../_shared/common-box-attributes.md).

## Messages

### bang

The [deferlow](https://docs.cycling74.com/reference/deferlow) object places the received message at the tail of the low priority queue for deferred execution.

### int

The [deferlow](https://docs.cycling74.com/reference/deferlow) object places the received message at the tail of the low priority queue for deferred execution.

Arguments:

- value
  [int]

### float

The [deferlow](https://docs.cycling74.com/reference/deferlow) object places the received message at the tail of the low priority queue for deferred execution.

Arguments:

- value
  [float]

### list

The [deferlow](https://docs.cycling74.com/reference/deferlow) object places the received message at the tail of the low priority queue for deferred execution.

Arguments:

- args
  [list]

### anything

The [deferlow](https://docs.cycling74.com/reference/deferlow) object places the received message at the tail of the low priority queue for deferred execution.

Arguments:

- args
  [list]

## Output

### anything

Same as the input.

## See Also

| Name | Description |
| --- | --- |
| [defer](https://docs.cycling74.com/reference/defer) | Defer execution of a message |
| [delay](https://docs.cycling74.com/reference/delay) | Delay a bang |
| [qlim](https://docs.cycling74.com/reference/qlim) | Queue-based message limiter |
| [uzi](https://docs.cycling74.com/reference/uzi) | Send many bang messages |
