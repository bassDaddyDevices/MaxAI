---
type: max
name: "!/"
aliases: ["rdiv"]
summary: "Divide input from a number"
signal: false
url: "https://docs.cycling74.com/reference/!%2F/"
package: "Max"
see_also: ["/", "expr"]
---
# !/

Divide input from a number

## Description

Divides a number by the incoming value. Functions like the [/](https://docs.cycling74.com/reference/div) object, but the inlets' functions are reversed.

## Arguments

### divisor[number] optional

Sets an initial value for the divisor. If there is no argument, the divisor is set to 1 initially. A float argument causes the numbers to be divided as floats.

## Attributes

### Common Box Attributes

Shared across all objects — see [Common Box Attributes](../_shared/common-box-attributes.md).

## Messages

### bang

In left inlet: Performs the division with the numbers currently stored.

### int

Stores the value as the divisor (the number to be divided into the number in the right inlet) and produces output.

Arguments:

- input
  [int]

### (inlet1)

Numbers received in the right inlet are divided by numbers received in the number in the left inlet.

Arguments:

- input
  [int]

### float

Converted to  int , unless [!/](https://docs.cycling74.com/reference/rdiv) has a float argument.

Arguments:

- input
  [float]

### (inlet1)

Converted to  int , unless [!/](https://docs.cycling74.com/reference/rdiv) has a float argument.

Arguments:

- input
  [float]

## Output

### float

Only if there is an argument with a decimal point.

### int

The two numbers in the inlets are divided, and the result is sent out the outlet.

## See Also

| Name | Description |
| --- | --- |
| [/](https://docs.cycling74.com/reference/div) | Divide two numbers |
| [expr](https://docs.cycling74.com/reference/expr) | Evaluate a mathematical expression |
