---
type: msp
name: "twist~"
summary: "Make Linear Ramps Curved"
signal: true
url: "https://docs.cycling74.com/reference/twist~/"
package: "MSP"
see_also: ["curve~", "ramp~", "shape~"]
---
# twist~

Make Linear Ramps Curved

## Description

The [twist~](https://docs.cycling74.com/reference/twist~) object accepts a ramp between 0 and 1 (such as one generated by [phasor~](https://docs.cycling74.com/reference/phasor~)) and produces a piecewise linear approximation of an expoential function using the same algorithm as the [curve~](https://docs.cycling74.com/reference/curve~) object. The curvature of the ramp is controlled by the curve attribute that ranges from -1 to 1. A curve value of 0 produces an output ramp identical to the input.

## Arguments

None.

## Attributes

### curve[float]

The curve attribute sets the parameters of the exponential function that generates the non-linear output. It can range between -1 and 1. For ascending ramps, values of curve less than 1 produce values above the linear input and values of curve greater than 1 produce values below the linear input. As curve values near -1 or 1, the curavture becomes more extreme.

### shapemode[int]

The shapemode attribute, when enabled, causes descending ramps to be symmetrical with ascending ramps. shapemode is enabled by default.

### syncupdate[int]

When enabled syncupdate defers changes to the curve attribute until either an input ramp starts or changes direction (resets). This avoids discontinuities in the ramp output.

### Common Box Attributes

Shared across all objects — see [Common Box Attributes](../_shared/common-box-attributes.md).

## Messages

### int

TEXT\_HERE

### float

TEXT\_HERE

### signal

Connect any object producing a signal ramp between 0 and 1.

### test

TEXT\_HERE

## See Also

| Name | Description |
| --- | --- |
| [curve~](https://docs.cycling74.com/reference/curve~) | Exponential ramp generator |
| [ramp~](https://docs.cycling74.com/reference/ramp~) | Trigger a Single Ramp With an Audio Signal |
| [shape~](https://docs.cycling74.com/reference/shape~) | Time-scaled Breakpoint Envelope Generator |
